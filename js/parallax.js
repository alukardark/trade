new simpleParallax(document.querySelectorAll('.parallax--05'), {
    delay: 0,
    orientation: 'down',
    scale: 1.4,
    overflow: true,
});

new simpleParallax(document.querySelectorAll('.parallax--0'), {
    delay: 0,
    orientation: 'down',
    scale: 1.8,
    overflow: true,
});



new simpleParallax(document.querySelectorAll('.parallax--2'), {
    delay: 0,
    orientation: 'up',
    scale: 1.8,
    overflow: true,
});


new simpleParallax(document.querySelectorAll('.parallax--6'), {
    delay: 0,
    orientation: 'up',
    scale: 1.8,
    overflow: true,
});

new simpleParallax(document.querySelectorAll('.parallax--3'), {
    delay: 0,
    orientation: 'up',
    scale: 1.8,
    overflow: true,
});
new simpleParallax(document.querySelectorAll('.parallax--4'), {
    delay: 0,
    orientation: 'up',
    scale: 1.8,
    overflow: true,
});




new simpleParallax(document.querySelectorAll('.parallax--7'), {
    delay: 0,
    orientation: 'up',
    scale: 2.1,
    overflow: true,
});

new simpleParallax(document.querySelectorAll('.parallax--8'), {
    delay: 0,
    orientation: 'up',
    scale: 1.3,
    overflow: true,
});
new simpleParallax(document.querySelectorAll('.parallax--9'), {
    delay: 0,
    orientation: 'up',
    scale: 1.5,
    overflow: true,
});
